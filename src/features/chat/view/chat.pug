include ./header/header.pug
include ./chatList/chatList.pug
include ./ui/input/input.pug
include ./ui/button/button.pug

.chat-page.flex
    header.header.flex
        +header("chatHeader")
    main.main.flex
        +chatList("chatList")
        section.header-offset.view-chat.flex
            +header("viewHeader")
            +input("input-message__text-field")
        section.header-offset.group-info.flex
            +header("group-info")